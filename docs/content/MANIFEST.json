{
  "$comment": "Where each piece of material goes. Written as it is produced, not mined later — the reasoning is freshest the day it is discovered, and a finding nobody routed is a finding nobody uses.",
  "$destinations": {
    "landing": "A section of the marketing site (docs/WEBSITE-AND-DOCS.md defines the seven).",
    "faq": "A question a buyer or developer actually asks.",
    "docs": "A page in the technical documentation.",
    "blog": "Worth a post of its own — a measured finding with a reproduction that nobody else can publish.",
    "internal": "Deliberately not published. Needs a reason."
  },
  "material": [
    {
      "id": "F-1",
      "what": "anon could TRUNCATE every tenant table on a default Supabase project",
      "to": ["blog", "landing", "faq"],
      "angle": "The strongest single artifact keelblock has. It affects every project inheriting Supabase's defaults, RLS does not cover TRUNCATE, and the reproduction is four lines."
    },
    {
      "id": "F-2",
      "what": "the leading generated RLS suite confirms a total cross-tenant leak as green",
      "to": ["blog", "landing", "docs"],
      "angle": "Why keelblock has two policy layers. The tool is excellent and states its own limit; the demonstration is what makes the argument."
    },
    {
      "id": "F-3",
      "what": "FORCE ROW LEVEL SECURITY does not stop the owner on Supabase",
      "to": ["blog", "docs"],
      "angle": "Corrects advice the tooling itself prints. Narrow, verifiable, useful to anyone on Supabase."
    },
    {
      "id": "F-4",
      "what": "a cross-tenant write is invisible to the attacker",
      "to": ["docs", "faq"],
      "angle": "Explains why read-based isolation tests cannot work, in one sentence."
    },
    {
      "id": "F-5",
      "what": "a failing USING on UPDATE is a silent no-op",
      "to": ["docs"],
      "angle": "Shapes every write test anyone writes here."
    },
    {
      "id": "F-6",
      "what": "Cache Components break every authenticated page in Next 16",
      "to": ["blog", "docs"],
      "angle": "Anyone adopting Next 16 with auth hits this. The trilemma table is the whole post."
    },
    {
      "id": "F-7",
      "what": "EXECUTE on a new function defaults to PUBLIC",
      "to": ["docs"],
      "angle": "One line in the tenant-table recipe."
    },
    {
      "id": "F-8",
      "what": "an unconstrained INSERT policy made the access matrix cry wolf",
      "to": ["blog", "docs"],
      "angle": "Our own defect, and the reason a matrix with a false positive is worse than none."
    },
    {
      "id": "F-9",
      "what": "an admin could demote the owner and seize the organization",
      "to": ["blog", "docs"],
      "angle": "Ours. Pairs with F-10 as the argument for auditing a suite you wrote yourself."
    },
    {
      "id": "F-10",
      "what": "the last owner could orphan an organization",
      "to": ["docs"],
      "angle": "Pairs with F-9."
    },
    {
      "id": "F-11",
      "what": "auth.uid() reads two settings and tools clear only one",
      "to": ["internal"],
      "angle": "Too specific to one tool's fixture behavior to be broadly useful; it is documented where it matters, in the gate."
    },
    {
      "id": "F-12",
      "what": "a deferred constraint trigger is invisible to a statement-level test",
      "to": ["docs"],
      "angle": "A correct protection no test can observe is indistinguishable from an absent one."
    },
    {
      "id": "F-13",
      "what": "keelblock's own unit gate could not fail",
      "to": ["blog", "landing", "faq"],
      "angle": "The most credible thing we can publish, because it is against us. Publishing it is what makes the other twenty-one believable."
    },
    {
      "id": "F-14",
      "what": "an overclaimed guarantee, corrected",
      "to": ["docs"],
      "angle": "Scope note on what the triggers do and do not bind."
    },
    {
      "id": "F-15",
      "what": "in an app-layer model the other tenant's row is in memory before the check",
      "to": ["landing", "blog", "faq"],
      "angle": "THE explanation of why keelblock exists, and a competitor wrote the code that demonstrates it."
    },
    {
      "id": "F-16",
      "what": "strict CSP, static prerendering, working hydration — pick two",
      "to": ["blog", "docs", "faq"],
      "angle": "Explains keelblock's report-only default, and is genuinely useful to anyone shipping a CSP on Next."
    },
    {
      "id": "F-17",
      "what": "NEXT_PUBLIC_ on a credential publishes it, and nothing warns you",
      "to": ["blog", "docs"],
      "angle": "Short, alarming, true, and no kit examined checks for it."
    },
    {
      "id": "F-18",
      "what": "keelblock inherited two-majors-behind rot from create-next-app on day two",
      "to": ["blog", "landing"],
      "angle": "The anti-rot argument, demonstrated on ourselves rather than asserted."
    },
    {
      "id": "F-19",
      "what": "a gate that misreports its own failure sends you to debug the wrong thing",
      "to": ["docs"],
      "angle": "Ours."
    },
    {
      "id": "F-20",
      "what": "the grep-versus-parse trap, three times, by the person who gated against it",
      "to": ["blog", "docs"],
      "angle": "Care demonstrably does not work; structure does. The most transferable lesson here."
    },
    {
      "id": "F-21",
      "what": "a specific date, from an agency blog, in a spec",
      "to": ["blog", "docs"],
      "angle": "Why the corpus exists. An unverifiable specific is more dangerous than an obvious guess."
    },
    {
      "id": "F-22",
      "what": "two records of one fact disagreeing, and the gate that hid it",
      "to": ["docs"],
      "angle": "Ours."
    },
    {
      "id": "F-23",
      "what": "documentation that asserts a count is documentation that will lie",
      "to": ["blog", "docs", "faq"],
      "angle": "The durable-versus-volatile rule, and a mechanism for it. This is the one every long-running project needs and almost none has."
    },
    {
      "id": "F-24",
      "what": "a test that touches shared state is the flake that disables the suite",
      "to": ["docs"],
      "angle": "Ours. Short, and the reasoning generalises to any test suite."
    },
    {
      "id": "F-25",
      "what": "two tracked deferrals silently deleted while every gate said ok",
      "to": ["blog", "docs"],
      "angle": "A register whose entries nothing references cannot detect its own losses. Generalises to any tracking system, and the failure is invisible by construction."
    },
    {
      "id": "F-26",
      "what": "the gate protecting keelblock's flagship evidence was indifferent to what the evidence said",
      "to": ["blog", "docs"],
      "angle": "The strongest self-criticism keelblock has published. A generated artifact needs two guarantees, freshness and content, and guarding only the first proves the document keeps up with the database while saying nothing about whether the database is acceptable. The fix generalizes: adjudication with a published reason, a reason floor so a token cannot retire a leak, and a stale allowance failing so the list cannot rot."
    },
    {
      "id": "F-27",
      "what": "both anomalies in the published access matrix were false positives, and REACHABLE was a claim the data could not support",
      "to": ["blog", "docs"],
      "angle": "Grep-versus-parse (F-20) inside a third-party tool, deciding what our own artifact says — plus the distinction between 'this assertion failed' and 'this boundary is open'. The lesson is that the word was kept loud and the dismissals were published with their reproductions, because softening a true signal to accommodate two false ones is how a matrix stops being read."
    },
    {
      "id": "F-28",
      "what": "a role that could destroy a table it could not read",
      "to": ["blog", "faq", "docs"],
      "angle": "The sequel to F-1 and a sharper version of it: the revoke covered two roles out of three, so service_role kept TRUNCATE on every tenant table and held no DML. Affects every project that copied the F-1 fix. Also the general rule — a hardening statement applied to the objects that existed when it was written decays silently unless ALTER DEFAULT PRIVILEGES carries it forward."
    },
    {
      "id": "F-29",
      "what": "the committed database types could not be regenerated from the repository",
      "to": ["blog", "docs"],
      "angle": "A generated artifact is only evidence if it reproduces from the inputs in the repository; this one had recorded a property of one machine — pgTAP's own views, typed as part of the application schema. The failure lands on the newcomer, who is told an artifact is stale through nothing they did. Only a clean rebuild could have found it, because the gate compares the file to whatever the current stack produces."
    },
    {
      "id": "F-30",
      "what": "the check that certifies every other check was a substring search",
      "to": ["blog", "docs"],
      "angle": "The sharpest instance of grep-versus-parse this project has: the meta-gate enforcing 'every gate has been shown to fail' did so by looking for a word, which a TODO comment satisfies. Publishable precisely because every proof it guarded turned out to be real — the defect was that nothing could have told us otherwise."
    },
    {
      "id": "F-31",
      "what": "a guarantee inherited from a platform default is a coincidence with good uptime",
      "to": ["blog", "landing", "faq", "docs"],
      "angle": "The single best argument this project can make for its own thesis, and it cost one CI run to find. An assertion green on every local run failed the first time it ran anywhere else, because two builds of the same Supabase Postgres image disagree about what anon may touch. RLS still held; the layer before it did not. The F-1 fix survived the same change because it was written down rather than inherited — same repo, same class, two outcomes, one difference."
    },
    {
      "id": "F-32",
      "what": "the documentation was falsified by the commit that made it stale",
      "to": ["blog", "docs"],
      "angle": "The cleanest small illustration of the whole thesis, and self-inflicted: a README sentence was true when written and false for the duration of the push that falsified it. Counts drift slowly and a periodic check catches them; a state claim goes stale instantly, in the same commit as the action. The fix is a badge plus a narrow rule, and the honest note that a project claiming documentation drifts should expect to keep proving it."
    },
    {
      "id": "F-33",
      "what": "a claim that was true, and a verification that could not have known",
      "to": ["blog", "docs"],
      "angle": "The sharpest small lesson in the register and it came from an outside reader: a test that passes under both branches of the question it was cited to answer is not evidence for either. SQLSTATE 42501 covers both a missing privilege and an RLS refusal, so the assertion could not confirm which layer refused - and the migration comment presented that breadth as what made the change safe. Being right by accident is not being right."
    },
    {
      "id": "F-34",
      "what": "an auth cookie is written with cache headers the context cannot apply, and the obvious fix breaks sign-in",
      "to": ["blog", "docs"],
      "angle": "A cross-USER leak class — one user's session token served to another — in a line of code that looks correct, plus the measurement that stopped us shipping a fix that would have broken sign-in. The reproduction is three lines of output."
    },
    {
      "id": "F-35",
      "what": "a security property was written down, pinned by a passing test, and not in effect on the running stack",
      "to": ["blog", "docs"],
      "angle": "The sharpest version of the F-31 lesson, because it is one level up and the test was GREEN. Everyone who edits config and keeps a long-running local stack has this. The reproduction is two shell commands comparing a container start time to a file mtime."
    },
    {
      "id": "F-36",
      "what": "the session cookie shipped without Secure, and the fix everyone writes breaks sign-in locally with no error",
      "to": ["blog", "docs", "faq"],
      "angle": "Applies to every project on @supabase/ssr, whose DEFAULT_COOKIE_OPTIONS omits `secure` entirely. The NODE_ENV trap is the useful half — `next start` sets production, so the obvious one-liner silently drops every auth cookie over http."
    },
    {
      "id": "F-37",
      "what": "Next 16.3.4's docs name a testing helper the shipped package does not export",
      "to": ["docs"],
      "angle": "Minor on its own; the rule it earns is not. The installed package is the primary source about itself, and documentation describes intent."
    },
    {
      "id": "F-38",
      "what": "with Cache Components a route cannot refuse with an HTTP redirect — it returns 200 and page chrome",
      "to": ["blog", "docs", "faq"],
      "angle": "Applies to every Next.js app with cacheComponents and a redirect-based auth check, and none of them will notice: the page looks empty and the browser does navigate to sign-in. Four approaches measured, including one that fails the build. The honest half is that we corrected our own spec's wording rather than the measurement."
    },
    {
      "id": "F-39",
      "what": "a 429 throttle was reported to the user as 'a sign-in link is on its way', for the whole life of the feature",
      "to": ["blog", "docs", "faq"],
      "angle": "The argument for the journey layer in one defect: three green test layers could not see it, and the first browser walk found it immediately. Also a precise correction to a real security practice — anti-enumeration requires hiding whether an account exists, not hiding that the provider refused."
    },
    {
      "id": "F-40",
      "what": "a boolean helper returned NULL for a non-member — correct in every policy that used it, and silently wrong the first time it was used in an `if`",
      "to": ["blog", "docs", "faq"],
      "angle": "The sharpest illustration of why two test layers exist. In a policy USING clause Postgres reads NULL as a refusal, so the helper was right where it was used and no isolation test could see it; the generated prober mocks these helpers to true/false, so it structurally cannot observe what the real one returns. It took an acceptance test written before the function, in the first procedural caller, to surface a mint into an organization the caller had no membership in. Generalizes to any Postgres codebase with is_… helpers built on a nullable lookup rather than exists()."
    },
    {
      "id": "F-41",
      "what": "the exhaustive RLS prober covered one table while the gate reported three, because the gate counted exit codes instead of files",
      "to": ["blog", "docs"],
      "angle": "Self-inflicted, in the gate this project points at when it says isolation is proven rather than asserted — which is exactly why it is worth publishing. Each per-table invocation reconciled away the previous one's output, and --no-fail returns zero, so nothing failed. The lesson is the repository's own recurring one in its most expensive location: a check that cannot fail is indistinguishable from a check that passes, and the fix was to compare the claim against the directory. Ships with five mutation proofs, one replaying the exact one-file directory the defect produced."
    },
    {
      "id": "F-42",
      "what": "the no-test-ids rule pushed page objects onto roles, which collided with Next's route announcer — and the collision was a race that every local run won and the first CI run lost",
      "to": ["blog", "docs", "faq"],
      "angle": "Two things worth publishing. A good rule (accessible locators, no test ids) has a cost nobody warns you about: the framework also ships elements with roles, and `role=\"alert\"` is one of them. And a green local run can be evidence of a faster machine rather than of correct code — the announcer mounts after hydration, so the assertion was racing it. Measuring instead of inferring then turned up an unrelated accessibility defect: two new components created their live region at the same moment as its message, which is the shape that never announces."
    },
    {
      "id": "F-43",
      "what": "the check that certifies every other check enumerated them by filename, exempting the two rule-enforcing scripts that were not named `check-*` — and when pointed at them, its strictness rule condemned three working proofs",
      "to": ["blog", "docs"],
      "angle": "The sequel to F-41 and the more useful half: the fix for a check that counts attempts is to ask what it PRODUCED, and the question generalizes to every counting rule in a codebase. Also the counter-lesson nobody publishes — a false positive in a strictness check is worse than a gap, because the repair it demands is rewriting correct code. The rule said three mutation proofs exercised nothing; they went through a one-line helper. Believing it would have meant damaging working tests to satisfy a rule that was itself too narrow."
    },
    {
      "id": "F-44",
      "what": "the same test-versus-navigation race as F-42, in a different file, surfaced by CI on a documentation-only commit",
      "to": ["blog", "docs"],
      "angle": "The recurrence is what makes it publishable: once is bad luck, twice is a class. The rule that falls out is sharper than 'add a wait' — when the page is going to navigate, assert on the RESPONSE, not the DOM, because a response body is a fixed artifact and the DOM is a moving one. Also a second data point for the uncomfortable half of F-42: a green local run can be evidence of a faster machine rather than of correct code."
    },
    {
      "id": "F-45",
      "what": "the upgrade path's central assumption — that a new migration file always applies however far a project has diverged — was false the first time it was tested",
      "to": ["blog", "docs", "landing"],
      "angle": "The category's deepest problem, attacked with a real experiment rather than a claim, and the claim lost. `supabase migration up` refuses a fix whose version sorts before the buyer's own later work, which is the ORDINARY case. Publishable because the correction is small and the reasoning error was total, and because two further things broke that nobody predicts: a second checkout silently shares the first one's database (so the experiment nearly proved a fix had arrived when it was already there), and a generated artifact cannot be shipped by an upgrade at all — upstream's copy describes upstream's schema, and the buyer has tables upstream has never heard of."
    },
    {
      "id": "F-46",
      "what": "the acceptance bar for upgradability and the ADR describing the upgrade policy contradicted each other, and it only showed up when someone satisfied the bar literally",
      "to": ["blog", "docs"],
      "angle": "Two documents both written carefully, both reasonable, mutually unsatisfiable: the ADR promises selective adoption of security fixes, the bar demands the current suite pass — and the current suite tests features a selective adopter does not have. The resolution is a genuine design position worth stating: schema is cumulative and adopted whole, product code is the buyer's and is never touched. Schema is cheap to accept and expensive to skip; product code is the reverse."
    },
    {
      "id": "F-47",
      "what": "the guide for the task where people leak data taught the exact defect a gate had learned to catch hours earlier",
      "to": ["blog", "docs"],
      "angle": "A gate catching a defect is not the same as the defect being unlearned. The schema guard learned about missing FORCE ROW LEVEL SECURITY the same day, from a real table that shipped without it — and the flagship guide teaching people to write those tables was not read, so it would have kept teaching it. Measured rather than reasoned: a table built exactly as the guide said reports RLS forced? false, and separately has no grant at all, so it is also unreachable. One gap fails open and one fails closed, which is a useful pair."
    }
  ],
  "$differentiators": "Every spec that ships a competitive capability, written up the day it ships. A spec that is done or partial declares what it lets keelblock claim, what the field does instead, evidence that resolves, and a battlecard section that exists — or is listed in $noDifferentiator with a reason. Checked by scripts/check-content.mjs. This exists because SPEC-004's Server-Action rule shipped and routed nowhere: the findings pipeline keys on F-* ids, and a capability produces none by itself, so the gap could only be noticed by a person saying it out loud.",
  "differentiators": [
    {
      "spec": "SPEC-001",
      "claim": "Tenant isolation is enforced by row-level security, so a foreign tenant's row is never selected — there is no second step to forget.",
      "rivals": "BoxyHQ, the most-starred free kit in the category, isolates in application code: getApiKeyById fetches any tenant's row by id, and a separate guard the route must remember to call compares the tenant afterwards. Zero `create policy` statements across their schema.",
      "evidence": ["F-15", "F-2", "supabase/tests/intent/001-tenant-isolation.test.sql"],
      "battlecard": "Isolation is a database property, not an application convention",
      "artifacts": ["docs/ACCESS-MATRIX.md"],
      "oneLine": "_Every kit tells you it isolates tenants. Ask which layer refuses, and what"
    },
    {
      "spec": "SPEC-002",
      "claim": "Isolation is proven per table, per command, per identity, and the resulting access matrix is committed and regenerated byte-for-byte in CI.",
      "rivals": "Nobody in the field publishes isolation proof. MakerKit has some tests, Supastarter journey e2e, ShipFast none — and none publishes a matrix of who can read and write what, per role, per table.",
      "evidence": ["F-2", "F-30", "docs/ACCESS-MATRIX.md"],
      "battlecard": "The proof is published, and it can fail",
      "artifacts": ["scripts/access-matrix.mjs"],
      "oneLine": "_A test suite that cannot fail is a screenshot. Ask to see the mutation"
    },
    {
      "spec": "SPEC-003",
      "claim": "Values that are correct by default are pinned anyway, so a vendor changing a default is a failing build rather than a silent loss of a security property.",
      "rivals": "The field ships the generated config and inherits whatever the vendor chose. Every project inheriting Supabase's defaults inherits sessions that never expire and a password floor below the vendor's own recommendation.",
      "evidence": [
        "F-31",
        "supabase/migrations/20260908140000_revoke_anon_table_access.sql",
        "src/lib/auth/config.test.mts"
      ],
      "battlecard": "A security property that is inherited is not a property",
      "artifacts": ["scripts/check-freshness.mjs"],
      "oneLine": "_Ask which of their security properties are true because someone chose"
    },
    {
      "spec": "SPEC-004",
      "claim": "Every exported Server Action reaches an authorization call — resolved through the module graph, per action — or is declared public with a written reason, and the build refuses one that does neither.",
      "rivals": "Nothing in the category checks this. Next.js documents the exposure itself: an exported action is reachable by direct POST even if never imported, and a page-level check does not extend to it. Its own mitigations are described as reducing risk where an auth layer is missing, not as a boundary.",
      "evidence": [
        "F-15",
        "F-34",
        "scripts/check-boundaries.mjs",
        "scripts/check-boundaries.test.mts"
      ],
      "battlecard": "Every Server Action authorizes, and the build refuses one that does not",
      "artifacts": [
        "docs/AUTHORIZATION.md",
        "docs/content/blog/server-actions-are-public-endpoints.md"
      ],
      "oneLine": "_Your Server Actions are public POST endpoints. Ask what happens when"
    },
    {
      "spec": "SPEC-005",
      "claim": "The organization switcher is a view selection, not a security boundary — policies ask the database per request whether the caller is a member, so a forged or stale active organization returns nothing rather than someone else's rows.",
      "rivals": "The common design puts the active organization in the session or a JWT claim and has policies read it back, which makes the switcher part of the boundary and inherits a token's staleness: an access token lives up to an hour, so a revoked admin stays an admin until it turns over. Supabase's own RBAC guide recommends that pattern and never mentions the window.",
      "evidence": [
        "research/08-ORG-CONTEXT.md",
        "supabase/tests/intent/005-organizations.test.sql",
        "spec/SPEC-005-organizations-and-roles.md"
      ],
      "battlecard": "A switcher that is allowed to be wrong",
      "oneLine": "*Ask what their organization switcher does when it is wrong — and how long a revoked admin keeps their access.*",
      "artifacts": ["src/lib/orgs/active-org.ts"]
    },
    {
      "spec": "SPEC-006",
      "claim": "A removed member loses access on their very next request, not when their token expires — proven by a browser journey that fails if the removal is skipped. And the one row a stranger must read is exposed through a single function returning a named two-column type, not a policy that admits anon.",
      "rivals": "The usual invitation surface is a policy admitting `anon` plus an application-side filter on the token, which selects the row before deciding the caller was entitled to it — and the usual membership check is a JWT claim, which cannot be revoked before it expires. Supabase's own RBAC guide recommends the claim pattern and does not mention the window; its default access token lives up to an hour.",
      "evidence": [
        "research/09-INVITATION-BOUNDARY.md",
        "supabase/tests/intent/006-invitations.test.sql",
        "e2e/journeys/invitation.spec.ts",
        "spec/SPEC-006-invitations.md"
      ],
      "battlecard": "Revocation you can watch happen",
      "oneLine": "*Ask them to remove a signed-in teammate while you watch, and time how long the access lasts.*",
      "artifacts": [
        "supabase/migrations/20260908160000_invitations.sql",
        "src/lib/orgs/invitations.ts"
      ]
    },
    {
      "spec": "SPEC-013",
      "claim": "The upgrade path is tested rather than described: which files an upgrade may take is a list in code with a mutation proof that it never touches product code, and the strategy's own load-bearing assumption was measured false the first time a synthetic buyer was upgraded — and corrected in the ADR rather than worked around.",
      "rivals": "MakerKit documents `git pull upstream main` and its page does not mention merge conflicts. supastarter's own documentation states that updating \"will become harder\" with every change you make to your application, because you are rebasing your code onto theirs. Neither publishes a test that an upgrade works, and neither addresses what happens to generated artifacts that describe the buyer's schema rather than the vendor's.",
      "evidence": [
        "research/10-UPGRADE-PATH.md",
        "docs/adr/ADR-008-upgradability.md",
        "spec/SPEC-013-upgrade-path.md",
        "scripts/upgrade.test.mts"
      ],
      "battlecard": "An upgrade path that was tested before it was claimed",
      "oneLine": "*Ask what happens when they fix a security bug and you cloned in March — then ask to see the test.*",
      "artifacts": ["scripts/upgrade.mjs", ".github/workflows/check.yml"]
    }
  ],
  "documentation": {
    "SPEC-001": "CONTRIBUTING.md",
    "SPEC-002": "docs/TESTING.md",
    "SPEC-004": "docs/AUTHORIZATION.md",
    "SPEC-005": "docs/AUTHORIZATION.md",
    "SPEC-013": "docs/TESTING.md"
  },
  "$noDocumentation": {
    "SPEC-003": "The gates document themselves at the point of failure: every one names the file, the line and the fix, and AGENTS.md carries the table of what each catches. A separate page describing them would be a second copy that goes stale the first time a message is improved.",
    "SPEC-006": "Covered by CONTRIBUTING.md's authoring recipes and by the invitation flow being the product surface itself. Revisit when the first task guide for invitations exists — a stranger inviting a teammate follows the UI, not a page."
  }
}
