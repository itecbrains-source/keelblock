# Deferral registry

The single registry of work deliberately **not built**, each with a reason and a **machine-evaluable
trigger**. `npm run check` reads this file.

**Rule 0 — a deferral is scope you chose not to build. A defect is never a deferral.** If you broke
it, you fix it in the change that broke it. Filing your own breakage here and merging as "done" is
the one behaviour this registry exists to prevent.

**Keep it thin.** A registry that grows to hundreds of rows stops being read, and then it is a place
debt goes to be forgotten rather than tracked. If this list is longer than the feature set, the
discipline has failed its own purpose.

## How a trigger works

A trigger is evaluated on every `check`. **When it fires, the build fails** — that is the whole
mechanism. A deferral whose moment has arrived cannot sit quietly in a file; it must be built,
closed, or its trigger deliberately restated with a reason.

| Trigger                | Fires when                                         | Use for                          |
| ---------------------- | -------------------------------------------------- | -------------------------------- |
| `file-exists:<path>`   | the path appears in the repository                 | "when we have X"                 |
| `env-set:<NAME>`       | the environment variable is set                    | "when we have credentials for X" |
| `spec-done:<SPEC-NNN>` | that spec's status becomes `done`                  | "after X ships"                  |
| `date:<YYYY-MM-DD>`    | the date passes                                    | a dated commitment               |
| `decided:<token>`      | never automatically — a human records the decision | a genuine judgement call         |

`decided:` never fires on its own, so it is the honest choice when nothing observable marks the
moment — and it is the one to be suspicious of, because it is also the easiest place to hide
something indefinitely.

## Open

| id      | title                                                                            | reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | trigger                                    |
| ------- | -------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| DEF-001 | Deploy topology for keel itself                                                  | keel has no environments of its own. Every SPEC-016 preflight check takes a **named target** rather than keel inventing a deployment story for its users — a topology ADR written now would be recording a choice nobody has made. When keel gains a real deployment (a demo, or its own site), that decision becomes an ADR: which environments exist, how migrations reach each one, and what "released" means.                                                                                                                                                                                                                                                                                                     | `file-exists:.github/workflows/deploy.yml` |
| DEF-002 | Journey test layer (Playwright)                                                  | The fourth test layer in ADR-005 needs an authenticated flow to drive, and authentication is not built. Writing browser tests against a login page that does not exist would produce tests that pin nothing. When built: **accessible locators only** (SPEC-002 REQ-3b) and page objects behind Playwright fixtures — both settled in advance, because retrofitting locators across a written suite never happens.                                                                                                                                                                                                                                                                                                    | `spec-done:SPEC-004`                       |
| DEF-005 | Enterprise identity — SSO/SAML and SCIM directory sync                           | **Narrowed 2026-09-07:** audit logs, API keys and outbound webhooks came OUT of this row and into scope (SPEC-025/026/027), because all three are tenant-isolation surfaces and isolation is what keel claims — delegating them puts the product's central promise outside its own boundary. What remains is genuine enterprise identity. Still deferred for a real reason rather than ordering: **neither SSO nor SCIM can be verified without an identity provider to test against**, so building them now would produce exactly the unexercised code this project refuses. Delegate-not-implement is the right pattern for these two, and was the wrong one for the other three.                                   | `decided:enterprise_tier`                  |
| DEF-006 | Auth hardening — per-account login lockout, disposable-email blocking, bot check | Three anti-abuse measures identified from `boxyhq/saas-starter-kit` that keel should have and cannot build yet, because there is no login to protect and building lockout before login is the orphan pattern. The lockout one is **not redundant with Supabase**: its rate limiting is IP-based plus fail2ban, and pentesters have brute-forced individual accounts by rotating IPs — there are open Supabase discussions requesting per-account limits. Their shape: an attempt counter, a `lockedAt` timestamp, and an unlock token by email. Disposable-email blocking wants a bundled domain list; the bot check should be dependency-optional (a no-op until keys are set) so it never blocks local development. | `spec-done:SPEC-004`                       |
| DEF-008 | TypeScript 7                                                                     | `typescript-eslint` does not support TS 7 — ESLint aborts outright with "typescript-eslint does not support TS 7.0", tracked upstream at typescript-eslint#10940. This is a genuine external blocker, not a preference: keel is on TS 6, one major behind current, which is inside the freshness gate's one-major grace period, and the grace exists for exactly this situation. Found by the freshness gate on its first run, which also revealed keel had inherited `typescript: ^5` from `create-next-app` and was **two** majors behind on day two.                                                                                                                                                               | `decided:typescript_eslint_supports_7`     |
| DEF-009 | Pricing research before any number is published                                  | The tier STRUCTURE is decided (D-6); the numbers are not. Copying a competitor's price is the recollection-over-research failure this project has a gate against, and price is the number a buyer judges hardest. Needs its own memo: willingness to pay for compliance evidence, what SOC 2 tooling costs a startup today, and whether annual or one-time fits an obligation that recurs annually. Filed so a number cannot be quietly guessed onto a page.                                                                                                                                                                                                                                                          | `decided:pricing_researched`               |
| DEF-010 | Which gates ship to a buyer, and which stay keel's own governance                | The suite splits. Schema guard, boundaries, access matrix, freshness, generated artifacts, locale, unused, gate-health and status are useful to every project built on keel and should ship by default. The research corpus, content manifest, bar coverage and spec contracts govern **keel as a product** — a buyer may opt in, but forcing them would be precisely the fighting-the-boilerplate complaint the field is criticised for. The decision belongs with SPEC-011, which determines what a scaffolded project contains.                                                                                                                                                                                    | `spec-done:SPEC-011`                       |
| DEF-011 | Mutation testing over application logic (Stryker)                                | Every gate here already carries a hand-written mutation proof, which is the same idea where it matters most. Stryker generalises it to application logic — and there is none yet. Run over gate scripts it would be slow, noisy, and tell us what we already know. It earns its keep the moment money math exists, because that is where a passing test suite over subtly wrong arithmetic is most expensive.                                                                                                                                                                                                                                                                                                         | `spec-done:SPEC-007`                       |
| DEF-012 | Property-based testing (fast-check)                                              | Valuable exactly where a function has an invariant worth stating — splitting a payment conserves every cent; a permission check is never more permissive than its policy. keel has no such function yet, and adding it now would produce property tests over string formatting, which is theatre.                                                                                                                                                                                                                                                                                                                                                                                                                     | `spec-done:SPEC-007`                       |
| DEF-004 | Supabase clients are written but wired to nothing                                | `src/lib/supabase/{client,server,server-only/admin}.ts` exist and are imported by no production code — orphans by this project's own no-orphan-stubs rule. They are the substrate SPEC-004 (auth) consumes, and deleting them to re-add them a week later is churn rather than discipline. `knip` is exempted for that path, with the reason recorded in `knip.reasons.md`.                                                                                                                                                                                                                                                                                                                                           | `spec-done:SPEC-004`                       |
| DEF-003 | CI executed on a real remote                                                     | The workflow is structurally asserted by 12 tests and run locally by `npm run verify` at ~45% fidelity, but **has never executed on GitHub**. Under the project's own rule, an unrun workflow is a file, not a working gate. This closes on the first push to a remote.                                                                                                                                                                                                                                                                                                                                                                                                                                               | `decided:remote_created`                   |

## Closed

| id      | title                      | closed by                                                                                                                                                                        |
| ------- | -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEF-007 | App Router conventions ADR | [ADR-011](../docs/adr/ADR-011-app-router-conventions.md) — decided before the first real UI, so conventions are chosen rather than inherited from whatever the first screen did. |
