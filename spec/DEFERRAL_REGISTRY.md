# Deferral registry

The single registry of work deliberately **not built**, each with a reason and a **machine-evaluable
trigger**. `npm run check` reads this file.

**Rule 0 — a deferral is scope you chose not to build. A defect is never a deferral.** If you broke
it, you fix it in the change that broke it. Filing your own breakage here and merging as "done" is
the one behavior this registry exists to prevent.

**Keep it thin.** A registry that grows to hundreds of rows stops being read, and then it is a place
debt goes to be forgotten rather than tracked. If this list is longer than the feature set, the
discipline has failed its own purpose.

## How a trigger works

A trigger is evaluated on every `check`. **When it fires, the build fails** — that is the whole
mechanism. A deferral whose moment has arrived cannot sit quietly in a file; it must be built,
closed, or its trigger deliberately restated with a reason.

| Trigger                | Fires when                                         | Use for                          |
| ---------------------- | -------------------------------------------------- | -------------------------------- |
| `file-exists:<path>`   | the path appears in the repository                 | "when we have X"                 |
| `env-set:<NAME>`       | the environment variable is set                    | "when we have credentials for X" |
| `spec-done:<SPEC-NNN>` | that spec's status becomes `done`                  | "after X ships"                  |
| `date:<YYYY-MM-DD>`    | the date passes                                    | a dated commitment               |
| `decided:<token>`      | never automatically — a human records the decision | a genuine judgment call          |

`decided:` never fires on its own, so it is the honest choice when nothing observable marks the
moment — and it is the one to be suspicious of, because it is also the easiest place to hide
something indefinitely.

## Open

| id      | title                                                                            | reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | trigger                                    |
| ------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| DEF-001 | Deploy topology for keelblock itself                                             | keelblock has no environments of its own. Every SPEC-016 preflight check takes a **named target** rather than keelblock inventing a deployment story for its users — a topology ADR written now would be recording a choice nobody has made. When keelblock gains a real deployment (a demo, or its own site), that decision becomes an ADR: which environments exist, how migrations reach each one, and what "released" means.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `file-exists:.github/workflows/deploy.yml` |
| DEF-005 | Enterprise identity — SSO/SAML and SCIM directory sync                           | **Narrowed 2026-09-07:** audit logs, API keys and outbound webhooks came OUT of this row and into scope (SPEC-025/026/027), because all three are tenant-isolation surfaces and isolation is what keelblock claims — delegating them puts the product's central promise outside its own boundary. What remains is genuine enterprise identity. Still deferred for a real reason rather than ordering: **neither SSO nor SCIM can be verified without an identity provider to test against**, so building them now would produce exactly the unexercised code this project refuses. Delegate-not-implement is the right pattern for these two, and was the wrong one for the other three.                                                                                                                                                                                                                                                                                      | `decided:enterprise_tier`                  |
| DEF-006 | Auth hardening — per-account login lockout, disposable-email blocking, bot check | **Restated 2026-09-08.** The original reason — "there is no login to protect" — expired when SPEC-004 shipped one, and the trigger fired correctly. The new one is not a dodge and is stated so it can be judged: these three defend a login **strangers can reach**, and nothing outside this machine can reach this one. keelblock has no deployment (DEF-001), so per-account lockout today protects a login with no attackers against an attack nobody can mount. The measure that would be theatre is building it now and calling the product hardened. The substance is unchanged and still correct: Supabase's limits are per-IP plus fail2ban, pentesters brute-force individual accounts by rotating IPs, and the shape is an attempt counter, a `lockedAt`, and an unlock token by email.                                                                                                                                                                           | `file-exists:.github/workflows/deploy.yml` |
| DEF-008 | TypeScript 7                                                                     | `typescript-eslint` does not support TS 7 — ESLint aborts outright with "typescript-eslint does not support TS 7.0", tracked upstream at typescript-eslint#10940. This is a genuine external blocker, not a preference: keelblock is on TS 6, one major behind current, which is inside the freshness gate's one-major grace period, and the grace exists for exactly this situation. Found by the freshness gate on its first run, which also revealed keelblock had inherited `typescript: ^5` from `create-next-app` and was **two** majors behind on day two.                                                                                                                                                                                                                                                                                                                                                                                                             | `decided:typescript_eslint_supports_7`     |
| DEF-009 | Pricing research before any number is published                                  | The tier STRUCTURE is decided (D-6); the numbers are not. Copying a competitor's price is the recollection-over-research failure this project has a gate against, and price is the number a buyer judges hardest. Needs its own memo: willingness to pay for compliance evidence, what SOC 2 tooling costs a startup today, and whether annual or one-time fits an obligation that recurs annually. Filed so a number cannot be quietly guessed onto a page.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `decided:pricing_researched`               |
| DEF-010 | Which gates ship to a buyer, and which stay keelblock's own governance           | The suite splits. Schema guard, boundaries, access matrix, freshness, generated artifacts, locale, unused, gate-health and status are useful to every project built on keelblock and should ship by default. The research corpus, content manifest, bar coverage and spec contracts govern **keelblock as a product** — a buyer may opt in, but forcing them would be precisely the fighting-the-boilerplate complaint the field is criticized for. The decision belongs with SPEC-011, which determines what a scaffolded project contains.                                                                                                                                                                                                                                                                                                                                                                                                                                  | `spec-done:SPEC-011`                       |
| DEF-011 | Mutation testing over application logic (Stryker)                                | Every gate here already carries a hand-written mutation proof, which is the same idea where it matters most. Stryker generalises it to application logic — and there is none yet. Run over gate scripts it would be slow, noisy, and tell us what we already know. It earns its keep the moment money math exists, because that is where a passing test suite over subtly wrong arithmetic is most expensive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `spec-done:SPEC-007`                       |
| DEF-012 | Property-based testing (fast-check)                                              | Valuable exactly where a function has an invariant worth stating — splitting a payment conserves every cent; a permission check is never more permissive than its policy. keelblock has no such function yet, and adding it now would produce property tests over string formatting, which is theater.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `spec-done:SPEC-007`                       |
| DEF-004 | The browser Supabase client has no consumer                                      | **Narrowed 2026-09-08 — two thirds of this row expired rather than being satisfied.** It covered all three clients; `server.ts` is now consumed by the DAL and both action modules, and `response-client.ts` by the proxy and the auth callback. What remains is `src/lib/supabase/client.ts`, which genuinely has no production caller because every surface built so far is server-rendered. Its consumer is the first client-side data need — a realtime subscription or an optimistic update — which arrives with the in-app notification centre. Kept rather than deleted deliberately: the CSP already admits the Supabase origin for it, and a starter without a browser client is missing a thing its buyer expects. That is a judgment, so it is written here rather than assumed.                                                                                                                                                                                   | `spec-done:SPEC-020`                       |
| DEF-020 | Independent validation of the policy layer                                       | SPEC-002's Definition of Done carries a bar its author cannot clear: _someone other than the author planted a policy defect and confirmed the harness caught it._ The 2026-09-08 review planted defects in the GATES and found real ones; nobody has yet planted one in the POLICIES except the author, and `docs/review/05-VERIFICATION.md` says so in its own words. The distinction is the whole point of the bar -- a harness verified only by its author is verified against the same assumptions that would produce the bug, and every mutation proof in this repository was written by the person who wrote the thing it tests. This is not a defect and not scope: it needs a second competent person and roughly a day of their time, which is the cheapest credibility this project can buy and cannot be bought by writing more tests. Filed rather than left as an unticked box, because an unticked box on a spec marked `done` is a promise nobody is tracking. | `decided:independent_policy_review`        |
| DEF-021 | Codemods for breaking product-API changes                                        | ADR-008 promises codemods "for every breaking change, in the release, the way Next.js does it", and SPEC-013 ships the upgrade path without them. The reason is that **no breaking product-API change has happened yet**: keelblock is six specs old and nothing it exposes has been renamed or removed. A codemod written now would be written against an imagined change, which is the unexercised-seam problem ADR-002 refuses in another context -- and it would be untestable, because there is no before-and-after to transform. The design question is already settled by `research/10-UPGRADE-PATH.md` so it does not have to be rediscovered: a transform that cannot finish must leave the project failing to build, as Next's do ("Your build will error until these comments are explicitly removed"), rather than half-applying in silence. What is missing is a change to transform.                                                                            | `decided:first_breaking_product_change`    |
| DEF-022 | GitHub Security Advisories for the isolation-affecting class                     | ADR-008's fourth mechanism: a fix that matters should arrive as a notification rather than requiring the buyer to be watching the repository. It is not code -- it is a publishing act that needs a release process to hang from, and there isn't one: SPEC-016 (release preflight) is `draft`, and the first tag in this repository was cut today, for an experiment. Publishing an advisory against a project with no releases and no users (DEF-001) would be theatre. The trigger is the release process existing, not a date, because that is the thing actually missing.                                                                                                                                                                                                                                                                                                                                                                                                | `spec-done:SPEC-016`                       |
| DEF-016 | Review the pinned Supabase CLI version                                           | `supabase gen types` is a code generator, so the committed `database.types.ts` is a function of the CLI version as well as the schema — 2.117.0 parenthesizes conditional types that 2.109.0 does not, and `check-generated` compares byte-for-byte. CI is therefore pinned to 2.109.0, the version verified here. A pin nobody revisits is how a project becomes three majors behind, which is the exact rot this repository exists to refuse, so the pin carries a date rather than a good intention: bump it, regenerate the types, and commit both in one change.                                                                                                                                                                                                                                                                                                                                                                                                         | `date:2026-10-23`                          |
| DEF-017 | MFA (TOTP) and leaked-password protection                                        | Both are **Supabase Pro-plan features** — `config.toml` says so above `[auth.mfa]`, and leaked-password protection (HaveIBeenPwned) is documented as Pro and above. So the blocker is commercial, not technical: keelblock can write the configuration and the tests, and cannot make either behave on a free-tier install. Filed rather than scoped out because a reader of SPEC-004 will look for MFA and should find a reason instead of a silence, and because the honest failure here is the marketing one — listing a security control a default install does not have. When keelblock or its buyer is on a paid plan, both are configuration plus a pinned test, not a build. Distinct from DEF-005, which is enterprise identity (SSO/SAML, SCIM) and is blocked on having an identity provider to test against, not on a plan.                                                                                                                                       | `decided:supabase_paid_plan`               |
| DEF-018 | Live OAuth round-trip                                                            | The code path ships and is typed, and the sign-in surface renders a provider button only for providers named in `NEXT_PUBLIC_OAUTH_PROVIDERS` — so today it renders none, which is the honest state rather than a button that always fails. What cannot be done here is the part that matters: registering an application with a provider, holding its client secret, and driving a real consent screen. Until that exists, an OAuth test would assert that our code calls a function, which is not the thing that breaks. The magic-link half of REQ-7 IS verified end to end, through the real app against the local stack.                                                                                                                                                                                                                                                                                                                                                 | `env-set:NEXT_PUBLIC_OAUTH_PROVIDERS`      |
| DEF-019 | Custom SMTP sender for auth email                                                | Supabase caps auth email at **2 per hour, project-wide**, and is explicit that it "can only be changed with your own custom SMTP setup". For a product that signs in by emailed link that is a functional wall, not a hardening item — the third sign-in of an hour fails. It needs a sending domain and a provider account, neither of which exists yet, and configuring one against a fake domain would produce mail nobody receives. Documented in `.env.example` where an installer meets it. The same vendor page names the sibling failure to solve then: enterprise mail scanners fetch links before the recipient does, consuming a single-use link — the mitigation is a redirect through a domain we control, which is a decision this deferral carries with it.                                                                                                                                                                                                    | `env-set:SMTP_PASSWORD`                    |

## Closed

| id      | title                                                              | closed by                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEF-014 | Narrow tenant writes to RPCs, or accept the dual path deliberately | **DECIDED 2026-09-08: the dual path stays, and the rule that keeps it honest is now written down.** The trigger fired on SPEC-006 exactly as intended -- the row said deciding it against no application code "would be deciding it from imagination", and SPEC-005's `changeMemberRole`/`removeMember` plus SPEC-006's `accept_invitation` are that code. The rule adopted: **a definer RPC is used only where a policy CANNOT express the operation** -- creating an organization and its first owner in one transaction, and redeeming a token as somebody who is not yet a member. Everything a policy can express stays in the policy. Narrowing `organization_member` to RPCs would buy an assertable privilege-layer denial and pay for it in the currency SPEC-006 just showed is expensive: `invite_member` re-checked authority with `if not is_org_admin(...)` while that helper returned NULL for a non-member, and minted an invitation into a foreign organization with no exception (fixed in 20260908170000). A definer function moves the check from somewhere the database enforces to somewhere a human wrote, so adding two more of them to replace working policies would trade a proven boundary for a hand-written one. The direct path is gated on `is_org_admin` with the invariants a policy cannot state held by triggers, and `supabase/tests/intent/006` asserts the consequence that matters: a stranger's self-insert is refused 42501, so the invitation path is not optional. Recorded in ADR-001. |
| DEF-013 | Register the keelblock names (npm + domain)                        | All three held as of 2026-09-08, verified independently rather than taken: `keelblock.dev` by RDAP with a positive control, and npm `keelblock` + `create-keelblock-app` by the same 404 → 200 check that established they were free, both now published at 0.0.1 by `itecbrains`. The packages are honest placeholders — two files, a README that says it installs nothing useful yet, no code and no install scripts — because a name held by a package pretending to work is a worse claim than an unheld name. Bar B-1's headline command now points at something this project owns. The dated trigger never fired; it did its job by being dated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEF-015 | Publish the access matrix, and the run that produced it, at a URL  | Its premise expired rather than being satisfied. The deferral said a stranger must install five tools "before they can see it" — true of a private tree, false the moment the repository went public on 2026-09-08. The matrix renders at a URL, the runs that produce it are public and badged, and README's "Checking it without installing anything" now states the one fact that ties them: the `generated` gate compares the committed copy to a freshly generated one byte-for-byte, and CI runs it. Reproducing the proof still costs five tools and that is real; READING it and the run behind it costs none. The marketing surface for people who have not decided to care is SPEC-009's ordinary scope, not debt.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| DEF-003 | CI executed on a real remote                                       | Run 34190042576 on 2026-09-08, the first green CI run this project has had: `check`, `audit`, `codeql` and `secrets` all pass on github.com/itecbrains-source/keelblock. Twelve steps that `npm run verify` could only assert are now observed. It was not green first time — four runs found a real privilege boundary that depended on which Postgres image the CLI pulled (F-31), a gate leaving pgTAP behind for the next gate to trip over, and an unpinned code generator. Which is exactly what the deferral said: an unrun workflow is a file, not a working gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| DEF-002 | Journey test layer (Playwright)                                    | Built. `playwright.config.ts`, fixtures that own the database lifecycle, page objects, and seven journeys — the F-38 pin, the two-account isolation walk including the borrowed organization id, and one sign-in through the real form. Wired into `check` on every push rather than nightly, because the whole argument for this layer is that it sees what the other three cannot, and that is worth nothing a day late. It paid for itself while being written: seeding via `service_role` was refused `42501` by a posture the repository had deliberately stated, and the sign-in journey surfaced [F-39](../docs/FINDINGS.md) — a `429` reported to the user as a link on its way. The accessible-locator rule (SPEC-002 REQ-3b) finally has an acceptance criterion; it had none.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| DEF-007 | App Router conventions ADR                                         | [ADR-011](../docs/adr/ADR-011-app-router-conventions.md) — decided before the first real UI, so conventions are chosen rather than inherited from whatever the first screen did.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
