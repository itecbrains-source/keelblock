# Generated. Formatting these fights the tool that writes them, and the diff comes back next build.
.next
node_modules
.venv
next-env.d.ts
src/lib/db/database.types.ts
docs/ACCESS-MATRIX.md
docs/content/BATTLECARD.md
supabase/tests/rls
package-lock.json

# Playwright rewrites this after every run, without a trailing newline, so running the journey
# suite turned `format` red on the next `npm run check` — for a file nothing hand-edits.
test-results
